import React from 'react';
import styled from 'styled-components';
import * as stylesheet from './style/stylesheet';

const AppContainer = styled.div`
  margin: ${stylesheet.outermargin}
`;

const App = () => (
  <AppContainer>
    PLACEHOLDER
  </AppContainer>
)

export default App;
